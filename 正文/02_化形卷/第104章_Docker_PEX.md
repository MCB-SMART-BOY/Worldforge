# 化形卷 · 第104章 · Docker / PEX

尽管有onboarding文档，首次使用者仍需要配置Python、uv、系统依赖。季晚禾当初如果在Docker里跑——不需要花三个时辰排查为什么她机器上跑不通。

周栖野为`field-mapper`写了Dockerfile。`FROM python:3.11-slim`，安装依赖、复制代码、定义入口。Dockerfile是环境的完整声明——比任何README都更精确，因为它是可执行的。"Dockerfile不只说你需要Python 3.11——它说这是你怎么得到Python 3.11和所有依赖的完整方法。不是清单——是环境本身。"

季晚禾测试Docker版本。`docker run`——第一次就跑通了。没有venv，没有pip，没有"我的机器上..."。只有"在这个容器里它一定会跑"。

Docker让后来者的定义从"会用Python的人"扩展到"任何能用终端的人"。环境从隐式变成显式，从使用者的责任变成作者的交付物。入口的最后一公里——交付形式本身就是入口。

---
← [上一章：第103章_文档的入口](./第103章_文档的入口.md) · [目录](../../目录.md) · [下一章：第105章_林过川的旧石镜](./第105章_林过川的旧石镜.md) →
