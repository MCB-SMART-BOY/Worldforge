# 通界卷 · 第42章 · 路径的追踪

"追踪——在——单世界——中——是——'看——这个——请求——在——这个——服务——中——做了——什么'——在——多世界——中——是——'看——这个——请求——在——跨越——世界边界——时——每一座桥——是否——如——契约——所——声明——地——执行。'"

跨世界追踪——四个问题——被——回答——

"请求——走了——哪些——桥——实际的——路径——和——注册表中——的——预期——路径——是否——一致——如果——请求——走了——降级——路由——或——备用——桥——为什么——当时的——主桥——'不可用'——的——具体——原因——是什么——容断——事件——在——追踪中——被标注——'在——13:42:03——支付桥——断路器——断开——请求——降级——到——备用——支付桥——备用——支付桥——延迟——+120ms——降级——事件——ID——记录——在容断日志——中。'"

"请求——在——每一段——的——延迟——桥的——延迟——网关——的——延迟——sidecar——的——序列化——延迟——目标——世界——的——处理——延迟——这些——被——分段——测量——不是——总延迟——512ms——是——桥1——42ms——网关——8ms——桥2——67ms——目标处理——395ms——总——512ms——瓶颈——在——目标处理——目标处理——的——395ms——中——在——青炉——的——追踪——中——可以——进一步——分解——到——函数——级别——跨世界——追踪——的——索引——指向——青炉——追踪系统的——详细——报告——不是在——同一面板——是——同一个——请求——的——完整——故事——在——多个——面板——中——通过——追踪ID——连接。"

"错误——在——路径——的——哪一段——发生——错误码——错误——的——上下文——错误——时——的——容断——状态——错误——的——传播——到——哪些——上游——调用方——调用方——的——容断——如何——响应。不是——'请求——返回——500'——是——'请求——在——桥2——收到——错误——支付服务——返回——'余额——不足——但——实际上——支付服务——返回——的——是——'下游——数据库——超时'——桥——的——契约——验证——发现——错误——格式——不符合——输出契约——桥2——将此——错误——标注——为——'契约违规——不是——业务——错误'——返回——调用方——调用方——的——容断——不重试——不降级——直接——告警——因为——这是——未预期的——错误——格式。'"

"路径——的——状态——在——请求——的——整个——生命周期——中——被——最后——检查——请求——完成——后——的——实际——结果——和——注册表中——的——预期——路径——一致性——是否——符合——SLO——是否——存在——未——预期——的——降级——或——绕路——是否有——桥——的——版本——在——路径——中——与——注册表——声明的——当前版本——不一致。如果是——'此——路径——中——桥2——的——版本——是v2.0——注册表中——的——当前版本——是v2.1——此——不一致——可能——因为——sidecar——的——配置——未——同步——需要——检查。'不是——等到——告警——才——发现——是——在——追踪——中——每一次——请求——都——在——验证——路径——的——合规。"
