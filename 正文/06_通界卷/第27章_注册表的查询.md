# 通界卷 · 第27章 · 注册表的查询

"注册表——不是——只有管理员能看——是——任何人——在任何时候——查询——任何桥——任何版本——任何调用方——任何历史。"不是因为"透明"是好的——是因为——"在凌晨三点——当值班的人——需要知道——'这座桥——在什么前提下——认为自己是可用的'——不能等——'明天问顾迁舟'——必须——现在——在面板上——查到。"

注册表——支持——多维查询——

按桥——"查询——'支付桥'——当前版本——契约——proto——调用方列表——依赖桥列表——退场日期——容断策略——上次契约测试——结果——上次变更——时间——人——原因——当前状态——正常——降级——不可用。"

按调用方——"查询——'息壤推荐服务'——依赖哪些桥——每个桥——使用哪些字段——哪些版本——哪些容断——在哪些桥——即将退场——需要迁移。"

按世界——"查询——'从Python世界——到C++世界'——存在哪些桥——总共——多少——每座桥——协议——状态——退场日期——哪些桥——过期——未有续约。"

按字段——"查询——'哪些桥——的契约——使用——字段——user_id——int64——作为必填。'当一个字段的类型——需要——从int64迁移到string——注册表——能——告诉——所有受影响的桥——和——所有调用方。"

按变更——"查询——'过去三十天——哪些桥的契约——发生了MAJOR变更——变更——哪些——未经过契约测试——哪些——未经过所有调用方确认。'不是——'审计'——是——'让——变更的影响——被看到。'"

"注册表的查询——不只是面板——也有API——让——CI——自动化——容断演习——容量规划——都能——从注册表——获取——最新的——最准确的——关于桥——的——结构化数据。"API——返回的——不只是——proto文件——也包括——契约的元数据——"谁维护——谁依赖——何时退场——容断策略——SLO——所有这些——以JSON——以proto——以任何世界——能消费的格式——返回。不是——'人类可读的页面'——是——'机器可解析的结构——让——自动化——基于——注册表——的事实——做——判断——执行——容断——触发——通知。'"

"查询——让——注册表——从——'可能存在——但不知道在哪的——目录'——变成——'在任何时候——以任何方式——都能访问的——真相源。'不是——信息被锁在——一个人的——记忆——或——一个Excel——里——是——被——每一个人——在需要时——在不需要等待别人——的情况下——获取。"
