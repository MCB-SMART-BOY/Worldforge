# 守序卷 · 第4章 · Spring的魔法

"Spring————容器————不是————框架——是——在——运行时——为——你——做——了——所有——你没——有——写——在——代码——中的——事——依赖——注入——AOP——代理——事务——管理——缓存——安全——异步——事件——监听——这些——都——在——容器——启动——时——被——自动——装配——自动——配置——自动——代理——自动——生效——自动——在——你——不知道——的时候——把——你的——方法——调用——拦截——下来——在——前后——插入——额外的——逻辑——而——你的——代码——看起来——只是——一个——简单——的——方法——调用。"

"魔法——不是——坏的——魔法——是——当——它——按照——被——期望——的方式——运作——时——节省——了——无数——的——样板——代码——但——当——魔法——在——没有人——理解——它的——运作——方式——的——情况下——被——应用——被——升级——被——叠加——魔法——之上——再——有——魔法——每一个——自动——装配——的——bean——每一个——自动——代理——的——切面——每一个——自动——配置——的——属性——都是——一个——沉默——的——决策——做——决策——的——人——可能——不是——你——而是——Spring——Boot——的——自动——配置——类——它——根据——classpath——中——是否存在——某个——jar——决定——'应该——创建——这个——bean——使用——这个——默认——配置。'"

言知非——查了——常安——的——Spring——上下文——启动——日志——在——三百七十二——个——自动——配置——类——中——有两百四十一——个——被——匹配——被——激活——每一个——被——激活——的——自动——配置——都——意味着——"某个——开发者——在——某个——时间——在——pom.xml——中——加了——一个——dependency——Spring——Boot——自动——检测——到——这个——dependency——决定——'你应该——需要——这个——配置——我来——替你——做。'——那个——开发者——可能——只是——想要——dependency——中的——一个——工具——类——但他——得到了——整个——自动——配置——和他——不知道——的——默认——行为。"

"二十一年————pom.xml——中——积累了——更多的——dependency——有些——dependency——只是——为了——一个——过时——的——工具——而被——引入——但——它们——附带——的——自动——配置——仍然——在——生效——在——容器——启动——时——创建——不需要——的——bean——占用——不需要——的——连接——订阅——不需要——的——事件——增加——容器的——复杂性——和——启动——时间——和——没有人——能——解释——的——行为——因为——没有人——知道——一个——三年前——被——引入——的——dependency——附带——的——自动——配置——在——今天——是否——仍然——应该——被——激活。"

"Spring——的——魔法——把——决策——从——代码——中——移——到了——classpath——和——注解——和——自动——配置——类——和——条件——装配——的——复杂——逻辑——中——决策——仍然是——决策——但——它不再——被——写——在——一个——人——可以——读——到——的——地方——它在——运行时——被——动态——组装——除了——通过——容器——的——启动——日志——和——actuator——的——beans——端点——没有人——能看到——完整的——被——装配——出的——系统——是什么——直到——凌晨——它——在某——个——未被——注意——的——自动——配置——的——默认——行为——中——做出了——错误的——行为。"
