# 守序卷 · 第8章 · AOP的背面

"面向切面编程（Aspect-Oriented Programming）——AOP——是——Spring——的——核心——魔法——之一——它——允许——你——在——不——修改——原始——方法——的——情况——下——在——方法——调用——的前——后——或——环绕——插入——额外——的行为——事务——管理——安全——检查——日志——记录——缓存——监控——这些——都——可能——通过——AOP——被——织入——你的——代码——而——你的——代码——完全——不知道——它——被——织入——了什么。"

"在——常安——中——一个——简单——的——`accountService.getAccount(accountId)`——调用——实际——触发——了——以下——的——链——AOP——代理——拦截——方法——调用——`@Transactional`——切面——开启——事务——`@PreAuthorize`——切面——检查——权限——`@Cacheable`——切面——检查——缓存——未命中——继续——实际——方法——执行——`@Auditable`——切面——记录——审计——日志——方法——返回——值——`@CachePut`——切面——更新——缓存——`@Transactional`——切面——提交——事务——`@MetricsAspect`——切面——记录——耗时。在——这——条——AOP——链——中——每一步——都——可能——改变——方法——的——行为——或——消耗——额外——的——时间——在某——个——版本——升级——后——一个——新的——切面——可能——被——自动——加入——因为——某——个——dependency——的——自动——配置——在——容器——启动——时——自动——注册——了——一个——新的——切面——而这——切面——在——没有——人——知道——的——情况——下——开始——拦截——所有——的——`@Transactional`——方法——在——其中——加入——了——额外——的——连接——检查——检查——是否——有——活跃——的——数据库——连接——在——每个——事务——方法——被——调用——时——这个——检查——花费——5毫秒——在——一个——需要——调用——一千——次——事务——方法——的——批量——处理——中——增加——了——5秒——的——额外——延迟。"

"AOP——的——背面——是——代码——的——执行——路径——不再——和你——读到的——一致——你读到——`accountService.getAccount()`——以——为——它——只是——调用——这个方法——实际——上——它——调用——了——一个——代理——代理——调用——了——若干——个——切面——切面——最终——调用——了——你的——方法——然后——再——逆——向——通过——所有——切面——返回——给你——在——不同——的——运行时——条件——下——不同——的——切面——可能——被——激活——或——跳过——同——一个——方法——调用——的——行为——可——能——与——上——一次——调用——不同——这——种——不——确定——性——在——诊断——问题——时——是——最——大——的——障碍——因为——你——不能——只——看——源代码——你——需要——看——运行时——被——织入——后的——完整——链。"

言知非——在——常安——的——AOP——审计——中——建立了——每个——方法——的——切面——链——的可——追溯——性——"在代码——中——`@Transactional`——注解——旁边——注释——'此——方法——被——以下——切面——拦截——`TxAspect`（事务——管理）——`AuthAspect`（权限——检查）——`CacheAspect`（缓存——管理——如果——`@Cacheable`——存在）——`AuditAspect`（审计——记录）——完整——的——切面——链——在——`aop-chain-report`——中——对——应——的——条目——为——`AC-042`——在——每次——容器——启动——后——自动——生成——对——比——上次——启动——后——的——差异——差异——被——记录——被——通知。'"
