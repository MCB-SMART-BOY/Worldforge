# 守序卷 · 第3章 · 注解的沉默

言知非——继续追查——Ch1中——那个——超时——的——来源——`@Transactional`——在——`SettlementService.java`——的——`batchSettle`——方法——上——有一个——`@Transactional(readOnly = false, propagation = Propagation.REQUIRED)`——没有——timeout——属性——没有——rollbackFor——属性——什么都不——显式——只是——告诉Spring——"这个方法——需要——事务——默认——行为。"

"Spring的事务默认行为——是什么——propagation默认——是——REQUIRED——意思是——'如果——当前——有——事务——加入——如果——没有——创建——新的。'"isolation——默认——是——DEFAULT——意思是——'使用——底层——数据库——的——默认——隔离——级别。'——数据库——的——默认——隔离——级别——是什么——MySQL——21年前——是——REPEATABLE_READ——现在——还是——REPEATABLE_READ——但——二十一年前——的——MySQL——版本——对——REPEATABLE_READ——的实现——和——今天——不同——间隙锁（gap lock）——的——行为——变过——两次——但——没有人——在——升级——MySQL——版本——时——重新审视——@Transactional——的——默认——行为——是否——仍然——合适。"

"这些——默认——值——不在——任何——文档——中——不在——任何——配置——文件中——它们——在——Spring——的——源码——中——在——Hibernate——的——文档——中——在——MySQL——的——官方——手册——中——分散——在——不同——的——项目——不同——的——版本——不同——的——维护——者——之间——没有——人——能——在同一——时刻——知道——'当——这个——`@Transactional`——注解——被——应用——到——这个——方法——上——实际——生效——的——行为——是——什么。'"

"注解——是——写在——代码——中——的——沉默——假设。`@Transactional`——假设——'事务——的——超时——由——底层——连接池——决定——我不——需要——指定。'`@Cacheable`——假设——'缓存——的——失效——策略——由——缓存——管理器——决定——我在——写——这个——注解——时——的——缓存——策略——在——未来——仍然——适用。'`@Autowired`——假设——'Spring——容器——中——只有——一个——符合——类型——的——bean——如果——有——多个——Spring——会——报错——我不——需要——担心——歧义。'——这些——假设——在——注解——被——写——下——的——当时——是——对的——但——在——二十一年——的——演化——中——那些——前提——可能——已经——被——改变——了——而——注解——自己——不会——告警——不会——自动——重新——验证——不会——说——'我的——前提——变了——请——重新——审视——我。'"

"注解——的——沉默——是——Java——生态——的特——有——的——问题——在C++世界——没有——注解——每一个——行为——都需要——显式——写——在——代码——里——在——Python——世界——有——decorator——但——decorator——是——函数——可以——被——读——被——测试——在——Go——世界——没有——注解——没有——魔法——在——Rust——世界——derive——宏——在——编译期——展开——展开——的——代码——可以——被——看到——Java——的——注解——在——运行时——被——反射——处理——处理——的——逻辑——在——Spring——的——源码——中——在——AOP——的——代理——中——在——字节码——增强——中——你看不到——除非——你——去——读——Spring——的——源码。"

"但——每一个——依赖——常安——的——人——和——依赖——常安——的——人——的——后来——者——不需要——读——Spring——的——源码——他们——只需要——知道——'当——这个——`@Transactional`——在——这个——方法——上——在此刻——的——生产——环境中——实际——的——行为——是——什么——它的——超时——从哪里——来——在——什么——前提下——可能——改变。'"
